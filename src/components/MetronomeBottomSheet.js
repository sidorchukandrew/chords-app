import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

export default function MetronomeBottomSheet({visible, onDismiss}) {
  return (
    <View>
      <Text>MetronomeBottomSheet</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
