import React, {useState} from 'react';
import {StyleSheet, Text, View} from 'react-native';

export default function PerformSetlistScreen() {
  const [songs, setSongs] = useState(() => []);
  return (
    <View>
      <Text></Text>
    </View>
  );
}

const styles = StyleSheet.create({});
